# TaoyuanTime 上台展示測試情境

這份文件是給「上台展示」使用的，不是工程規格書。目標是讓觀眾快速理解：

- 這個專案想解決什麼問題
- 系統如何從活動資料收集到 LINE 回覆
- AI 在其中扮演什麼角色
- 使用者實際操作時會看到什麼
- 專案目前的亮點與限制

---

## 一、展示主軸

本專案是一個「桃園公共活動智慧推薦系統」。

它會自動整理不同來源的活動資料，透過 AI 協助補齊資訊、加上標籤與摘要，再把活動用 LINE Bot 推薦給使用者。使用者不用自己到很多網站查資料，只要在 LINE 裡用日常語氣詢問，就能取得活動卡片、查看詳情、導航、加入行事曆，甚至訂閱活動通知。

一句話介紹：

> 我們把分散在各網站的桃園活動資訊，整理成可以被 LINE Bot 查詢、推薦與訂閱的智慧活動服務。

---

## 二、上台展示前準備

### 必備環境

1. Django server 已啟動。
2. ngrok 已連到本機 `8000`。
3. LINE Developers webhook 已設定到目前 ngrok 網址。
4. `.env` 的 `PUBLIC_BASE_URL` 是目前 ngrok 網址。
5. 後台可開啟：

```text
http://127.0.0.1:8000/dashboard/
```

### 建議先確認

```powershell
python manage.py check
python manage.py test tests.test_line_semantic_query
```

展示前可以先用自己的 LINE 帳號測一次：

```text
推薦活動
有沒有腳踏車活動
免費嗎
更多
數位市民卡
```

---

## 三、最佳展示劇本總覽

建議展示順序：

1. 後台總覽：展示系統不是只有 LINE，而是有資料管理後台。
2. 爬蟲更新：展示資料來源可以被自動整理。
3. 活動卡片：展示整理後的活動如何被使用者看到。
4. LINE 推薦：展示個人化推薦與卡片互動。
5. AI 對話：展示自然語言查詢與追問。
6. 無結果處理：展示 AI 不亂編，會先否定再推薦相近活動。
7. 活動操作：展示查看詳情、導航、加入行事曆、訂閱。
8. 市民卡功能：展示系統可以延伸到城市服務。
9. 總結亮點與限制。

---

## 四、完整展示腳本

### 1. 開場

可以這樣說：

> 現在很多活動資訊散在不同網站，使用者要自己查、自己比對時間地點，也很容易錯過有興趣的活動。  
> 我們的系統會自動整理桃園活動資料，再透過 LINE Bot 讓使用者用聊天的方式查詢與訂閱活動。

接著打開後台 dashboard。

展示重點：

- 有活動資料
- 有使用者資料
- 有爬蟲任務
- 有 AI 處理紀錄
- 有活動健康度與連結檢查

---

### 2. 後台資料更新流程

打開：

```text
http://127.0.0.1:8000/crawlJobs/
```

可以說：

> 管理員不需要手動一筆一筆新增活動，可以透過後台的一鍵更新流程，讓系統去收集、匯入、檢查與整理活動。

展示流程：

1. 進入爬蟲任務頁。
2. 說明「一鍵完整更新」會做哪些事。
3. 如果不想現場真的跑很久，可以展示既有任務結果頁。

一鍵完整更新包含：

- 爬蟲收集活動
- 匯入資料庫
- 過期活動自動下架
- OCR 讀取海報文字
- AI 補欄位與加標籤
- 活動摘要生成
- LINE 卡片 readiness 檢查
- 官方連結健康檢查

展示說法：

> 這裡不是單純爬資料而已，系統還會判斷資料能不能推薦、能不能顯示在 LINE 卡片，以及官方連結是否失效。

---

### 3. LINE 推薦活動

在 LINE 輸入：

```text
推薦活動
```

預期效果：

- LINE 回傳活動卡片。
- 卡片包含圖片、標籤、時間、地點、摘要與操作按鈕。

可以說：

> 使用者不用打很精準的關鍵字，只要點推薦活動，系統就會根據偏好、近期活動與可推薦資料，整理成 LINE 卡片。

可以展示卡片上的按鈕：

- 活動詳細資訊
- 導航前往地點
- 加入行事曆
- 訂閱此活動通知
- 查看更多活動

---

### 4. 連續推薦與查看更多

接著輸入：

```text
更多
```

或再輸入：

```text
推薦活動
```

預期效果：

- 系統會延續上一輪推薦，換下一批活動。
- 不應一直重複同一組卡片。

展示說法：

> 這裡有做狀態記錄。使用者連續要更多活動時，系統會知道他是在看下一批，而不是每次都從第一批重新開始。

---

### 5. 自然語言查詢

輸入：

```text
我想找中壢的免費活動
```

預期效果：

- 系統會理解地區是中壢。
- 條件包含免費。
- 回傳符合條件或接近條件的活動卡片。

展示說法：

> 使用者不需要知道後台有哪些標籤，也不用填表單。他可以直接用平常講話的方式查活動。

---

### 6. 追問上一輪活動

看到卡片後，輸入：

```text
第一個是幹嘛
```

預期效果：

- 系統回答上一輪第一張活動的內容。
- 不會重新搜尋新活動。

接著輸入：

```text
免費嗎
```

預期效果：

- 系統回答上一輪活動或上一輪結果的費用狀態。
- 不會把「免費嗎」誤判成重新找免費活動。

展示說法：

> 這裡的難點是對話狀態。像「免費嗎」其實是在追問上一張卡片，不是要重新搜尋。系統會判斷這句話是在追問、精煉搜尋，還是要換一批。

---

### 7. 新主題切換

輸入：

```text
書法展在幹嘛
```

預期效果：

- 系統會把「書法展」當成新主題。
- 不應繼續追問上一張活動卡片。

展示說法：

> 如果使用者換了一個新主題，系統不能一直黏著上一張卡片。這是我們針對日常聊天情境調整過的地方。

---

### 8. 無結果但不亂編

輸入：

```text
有沒有腳踏車活動
```

預期效果：

如果沒有明確腳踏車活動，系統應先說：

```text
目前沒有找到明確符合「有沒有腳踏車活動」的活動，先推薦相似或其他活動給你。
```

接著才回傳相近活動卡片。

展示說法：

> 這裡很重要。AI 系統不能為了回答使用者就硬說有。沒有明確符合時，要先誠實說沒有，再推薦相似活動。

這個橋段很適合展示專案可信度：

- 不亂編活動
- 不假裝完全符合
- 還是提供下一步選擇

---

### 9. 活動詳細資訊與追蹤

點卡片上的：

```text
活動詳細資訊
```

預期效果：

- 使用者會被導到官方活動頁或系統中轉頁。
- 系統可以記錄點擊行為。

展示說法：

> 使用者點官方詳情時，不只是跳轉出去，系統也可以記錄這個互動，未來能用來改善推薦。

---

### 10. 導航與行事曆

點卡片上的：

```text
導航前往地點
```

預期效果：

- 開啟 Google Maps 或地點導航。

再點：

```text
加入行事曆
```

預期效果：

- 開啟 Google Calendar 新增事件連結。

展示說法：

> 這不是只有查詢資訊，而是把使用者從「看到活動」接到「真的要去」的流程。

---

### 11. 訂閱活動通知

點卡片上的：

```text
訂閱此活動通知
```

預期效果：

- LINE 回覆訂閱成功或已訂閱狀態。
- 使用者之後可收到活動提醒或異動通知。

展示說法：

> 使用者看到有興趣的活動後，可以直接訂閱。這樣活動快到了或資訊更新時，就不用自己再回網站查。

---

### 12. 數位市民卡展示

輸入：

```text
數位市民卡
```

如果尚未綁定，展示系統提示格式。

輸入測試碼：

```text
ABC12345678901
```

預期效果：

- 系統顯示確認綁定卡片。
- 按下確認後顯示綁定成功。
- 再輸入數位市民卡，顯示姓名、數位碼與條碼圖片。

展示說法：

> 這代表系統不只可以做活動推薦，也可以延伸到城市服務，例如市民卡、優惠、特約商店。

---

### 13. 市民卡特約商店

輸入：

```text
附近的市民卡特約商店
```

預期效果：

- 回覆中原附近特約商店卡片。

展示說法：

> 這裡展示的是活動之外的延伸服務。未來可以把市民卡優惠、活動優惠與地點推薦整合在一起。

---

## 五、展示時可以強調的專案特點

### 1. 資料來源整合

系統不是人工輸入單一資料，而是設計成可以整合多個活動來源。

包含：

- 政府活動資料
- 文化活動
- 觀光活動
- RSS 或 OpenAPI 資料
- 後台人工新增資料

重點說法：

> 公共資訊常常分散在不同網站，這個系統的第一步就是把分散資料集中整理。

### 2. 爬蟲後不是直接上架

爬蟲抓到資料後，系統還會做檢查與整理。

包含：

- 活動分類
- 日期地點整理
- 費用資訊判斷
- 圖片與海報處理
- OCR 讀取海報文字
- 品質分數
- LINE 卡片可用性檢查
- 官方連結是否失效

重點說法：

> 爬到資料只是第一步，重點是要整理成使用者看得懂、LINE 可以呈現、而且連結有效的活動資訊。

### 3. AI 協助整理資料

AI 不是拿來自由聊天，而是協助整理資料與理解使用者需求。

用在：

- 自動標籤
- 活動摘要
- OCR 後文字理解
- 使用者自然語言查詢
- 對話意圖判斷
- 活動排序

重點說法：

> AI 的角色是輔助整理與查詢，不是亂生成活動。回答必須回到資料庫裡的活動。

### 4. LINE 對話狀態判斷

系統會判斷使用者這一句話是：

- 追問上一張卡片
- 精煉搜尋條件
- 換下一批
- 開始新搜尋
- 換新主題
- 一般不支援聊天

重點說法：

> 真實使用者不會照規則講話，所以我們不是只靠固定指令，而是讓系統理解目前對話狀態。

### 5. 不亂編的回覆機制

如果沒有明確符合的活動，系統會先說沒有，再推薦相近活動。

重點說法：

> 對公共資訊服務來說，可信度很重要。沒有的活動不能硬說有。

### 6. 活動操作完整

每張活動卡片不是只有文字，還包含完整行動：

- 看詳細資訊
- 導航
- 加入行事曆
- 訂閱通知
- 查看更多

重點說法：

> 使用者不是只得到答案，而是可以直接採取下一步。

### 7. 有後台管理與任務紀錄

後台可以管理：

- 活動列表
- 爬蟲任務
- AI 處理紀錄
- 標籤審核
- 推播管理
- 使用者資料

重點說法：

> 這不是單一聊天機器人，而是一套有後台、有資料流程、有管理機制的系統。

---

## 六、專案優點

### 1. 使用門檻低

使用者只要有 LINE，就可以查活動、看推薦、訂閱通知。

### 2. 資料流程完整

從爬蟲、清理、AI 補資料、後台管理，到 LINE 呈現都有串起來。

### 3. 可展示性高

LINE 卡片、後台任務、活動詳情、導航、市民卡條碼都很適合現場展示。

### 4. AI 有實際用途

AI 不是只做聊天，而是用在標籤、摘要、搜尋理解與對話狀態判斷。

### 5. 有可信度設計

沒有明確活動時，系統會否定並推薦相近活動，不會硬編不存在的活動。

### 6. 可延伸到其他城市服務

目前以桃園活動為例，未來可以延伸到：

- 市民卡優惠
- 公共公告
- 報名提醒
- 活動異動通知
- 交通資訊

---

## 七、目前限制與可以改進的地方

這一段上台可以誠實講，反而會讓專案更完整。

### 1. 資料品質依賴來源網站

如果官方網站資料不完整，例如缺地點、缺費用、圖片失效，系統需要靠 AI 或後台補救。

### 2. 部分官方連結可能失效

系統已經有連結檢查機制，但來源網站如果刪頁或改網址，仍需要重新檢查與修正。

### 3. AI 判斷仍需要規則保護

自然語言很彈性，像「免費嗎」「這個在幹嘛」「還有嗎」都可能有不同意思，所以目前系統採用 AI 加規則一起判斷。

### 4. 交通資訊目前以 Google Maps 為主

目前展示重點是導航連結，還沒有完整整合公車、捷運、台鐵即時時刻。

### 5. 推薦仍可持續優化

目前推薦已經包含偏好、去重、更多活動與相近推薦，但未來可以加入更多行為分析，例如點擊率、訂閱率、活動熱門度。

---

## 八、建議現場測試台詞

### 開場一句話

> 我們的專題是把分散的桃園活動資訊，自動整理成可以被 LINE 查詢、推薦與訂閱的智慧活動系統。

### 展示爬蟲時

> 這裡可以看到管理員不用手動整理每一筆活動，系統會透過一鍵更新流程處理爬蟲、匯入、AI 補資料與連結檢查。

### 展示 LINE 推薦時

> 使用者只要輸入推薦活動，系統就會整理成 LINE 卡片，並提供查看詳情、導航、行事曆與訂閱。

### 展示 AI 對話時

> 使用者可以用日常語氣問，例如中壢免費活動、第一個是什麼、免費嗎，系統會依照上下文判斷是在追問還是重新搜尋。

### 展示無結果時

> 如果沒有明確符合的活動，系統不會硬說有，而是先告訴使用者沒有找到，再推薦相似或其他活動。

### 結尾一句話

> 這個系統的價值不是單一功能，而是把資料收集、AI 整理、LINE 互動與城市服務串成一個完整流程。

---

## 九、最推薦的現場操作順序

如果時間只有 5 到 7 分鐘，建議照這個順序：

1. 開後台 dashboard，說明這是一套完整系統。
2. 開爬蟲任務頁，展示一鍵更新與任務紀錄。
3. LINE 輸入 `推薦活動`，展示活動卡片。
4. LINE 輸入 `更多`，展示不重複推薦。
5. LINE 輸入 `第一個是幹嘛`，展示追問。
6. LINE 輸入 `有沒有腳踏車活動`，展示無明確結果時先否定再推薦。
7. 點卡片的導航或加入行事曆。
8. LINE 輸入 `數位市民卡`，展示城市服務延伸。

---

## 十、展示成功標準

展示成功不一定是每個功能都跑完，而是觀眾能理解：

- 這不是一般聊天機器人。
- 系統有資料來源與後台管理。
- AI 有用在整理資料與理解查詢。
- LINE 是使用者最容易接觸的入口。
- 系統有誠實回覆與相近推薦機制。
- 未來可以延伸成智慧城市公共資訊服務。

