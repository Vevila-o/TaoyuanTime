# ER Reference & Thumbnail

下列為本系統主要 ER 與資料表摘要。

- ER 檔（Mermaid）：`docs/tyaihub/diagrams/er-mvp-tables.mmd`（建議使用 mermaid 支援的工具或轉 PNG）
- 資料表摘要：請參考 `docs/tyaihub/database-schema.md` 或 `events/README_DB.md` 中的 Activity、Tag、SourceWebsite、UserProfile、Subscription、ActionLog 節。

若需視覺化圖片（PNG），請在本機用 mermaid 或 draw.io 轉出並放在 `docs/delivery/` 中（此交付版保留 mermaid 原始檔）。
